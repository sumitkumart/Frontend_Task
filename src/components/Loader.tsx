const Loader = () => {
  return (
    <div role="status" aria-live="polite" aria-label="Loading" className="loader">
      <span className="sr-only">Loading products...</span>
    </div>
  )
}

export default Loader

